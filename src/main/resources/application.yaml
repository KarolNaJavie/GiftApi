spring:
  datasource:
    url: jdbc:mysql://localhost:3306/giftApi_db_schema
    username: root
    password: root
    driverClassName: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
  application:
    name: giftApi
